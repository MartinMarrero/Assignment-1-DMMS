/**
 * Implementation file to provide DecisionMethod key function(s) so the
 * vtable and typeinfo are emitted in exactly one translation unit.
 */

#include "./headers/decisionmethod.h"

DecisionMethod::~DecisionMethod() = default;
